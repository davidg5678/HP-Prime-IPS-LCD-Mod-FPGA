# captures/

Logic-analyzer capture artifacts land here starting in Phase 1. Contents are gitignored
(binary, regenerable-by-recapture, and potentially large) except for this file — only
`fixtures/` (small, hand-picked known-good captures used as Phase 2 test inputs) should
ever be committed, and only if kept intentionally small.
